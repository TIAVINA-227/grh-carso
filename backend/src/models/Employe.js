// Employe model removed (project standardized on Prisma)
// If some code still imports this file, the import will throw to surface the leftover usage.

export default function _throwModelImport() {
  throw new Error("Mongoose model 'Employe' removed — use Prisma via services/employeService.js instead.");
}
